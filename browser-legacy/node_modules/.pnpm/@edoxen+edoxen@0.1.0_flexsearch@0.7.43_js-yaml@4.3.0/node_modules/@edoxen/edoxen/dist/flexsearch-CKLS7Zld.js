let r = null;
async function l() {
  return r || (r = import("flexsearch").then((n) => {
    const e = n;
    return e.Document ? e : e.default ?? e;
  })), r;
}
async function u(n, e) {
  const a = await l(), i = new a.Document({
    document: { id: e.idField, index: e.fields },
    tokenize: "forward"
  });
  for (const d of n) i.add(d);
  return {
    search(d) {
      if (!d) return n;
      const f = i.search(d), t = /* @__PURE__ */ new Set();
      for (const c of f)
        for (const o of c)
          typeof o == "number" ? t.add(o) : o.doc && t.add(o.doc[e.idField]);
      return n.filter((c) => t.has(c[e.idField]));
    }
  };
}
export {
  u as createFlexSearchBackend
};
