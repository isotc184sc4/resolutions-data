import { getBodyTypes as getBodyTypesGlobal, BodyType, BodyCode } from '../body/index.js';
import { getProfile as getProfileGlobal, pickExtensionAttribute as pickExtensionAttributeGlobal, decodeProfile as decodeProfileGlobal, ProfileShape } from '../contacts/index.js';
export interface EdoxenConfig {
    /** Unique per site (e.g. 'tc184sc4', 'oiml', 'demo1'). */
    readonly scope: string;
    /** Default locale for pickLocalization calls. */
    readonly defaultLocale?: string;
    /** Body types for sites that host multiple committees. */
    readonly bodyTypes?: BodyType[];
    /** Profile registrations for `extensions[]` decoding. */
    readonly profiles?: Record<string, ProfileShape>;
}
export interface EdoxenConfigHandle {
    readonly config: EdoxenConfig;
    bodyTypeFromCommittee(committee: string | null | undefined): BodyCode | null;
    bodyTypeFromUrn(urn: string | null | undefined): BodyCode | null;
    pickExtensionAttribute(extensions: Parameters<typeof pickExtensionAttributeGlobal>[0], key: string): ReturnType<typeof pickExtensionAttributeGlobal>;
    decodeProfile<T extends ProfileShape>(extensions: Parameters<typeof decodeProfileGlobal>[0], profile: string, shape: T): ReturnType<typeof decodeProfileGlobal<T>>;
}
export declare function configureEdoxen(opts: EdoxenConfig): EdoxenConfigHandle;
export { getBodyTypesGlobal as getBodyTypes, getProfileGlobal as getProfile };
//# sourceMappingURL=index.d.ts.map